"""
1.5 Практикуемся в написании CSS-селекторов

Базовый синтаксис
-----------------
tag                     элемент по тегу: input
.class                  элемент по классу: .form-control
#id                     элемент по id: #submit_button
[attribute]             наличие атрибута: input[required]
[attribute="value"]     точное значение: input[type="email"]
A B                     потомок любой глубины: form button
A > B                   прямой потомок: form > button
A.class1.class2         оба класса у одного элемента: input.form-control.first

Порядок выбора локатора
-----------------------
1. data-testid или другой стабильный тестовый атрибут;
2. уникальный id;
3. короткий CSS-селектор с осмысленным атрибутом;
4. XPath — когда CSS не позволяет выразить условие.

Хороший локатор описывает смысл нужного элемента и выбирает ровно один элемент.
Например, input.second[required] точнее, чем input.second: на форме регистрации
второй селектор может выбрать необязательное поле адреса, а первый — только
обязательное поле фамилии.
"""
