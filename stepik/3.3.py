"""
3.3 Тестирование с помощью PyTest

Минимальный тест
---------------
def test_sum():
    assert 2 + 2 == 4

Pytest по умолчанию ищет файлы test_*.py или *_test.py, функции test_* и
классы Test* без собственного __init__. Запуск из корня проекта:

pytest
pytest -v
pytest -k login

Фиксация окружения
------------------
Зависимости сохраняем в requirements.txt. Это делает запуск воспроизводимым:

pytest==8.3.2
selenium==4.46.0

Проверки
--------
Обычный assert показывает выражение и фактические значения. Дополняем его
сообщением, когда без него причина ошибки неочевидна:

assert "Success" in message, f"Unexpected confirmation text: {message!r}"

Не маскируем ошибку try/except без повторного raise: тест должен падать там,
где перестало выполняться его ожидание.
"""
